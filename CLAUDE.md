# autobattler

8-player PvP auto-battler. Server-authoritative with a shared deterministic simulation core used by both server and clients.

## Hard invariants

- `packages/sim` is pure: no I/O, no `Math.random`, no `Date`, no floats
- All arithmetic uses integer fixed-point math (scale 1000 = 1.0)
- All randomness goes through the seeded mulberry32 PRNG in `prng.ts`
- All tuning numbers (unit stats, trait effects, item stats, economy) live in `packages/data`; content lives entirely in data (no content logic in code)
- `packages/balance` is the ONLY package permitted I/O (its CLI writes the report files); `sim`/`rules` stay pure
- Every change must keep determinism tests green (`npm test`)
- Pool conservation: total unit copies across pool + all player benches/boards is constant. PvE mobs are NOT drawn from the unit pool and items are NOT pooled (neither affects conservation)
- PvE loot is seeded-deterministic: orbs and their contents derive entirely from the match prng + loot.json (same seed + config → identical orbs/rewards); the future client only animates the reveal
- Command legality is enforced server-side in `packages/rules`; invalid commands return typed errors, never throw
- Server result is canon; client sim is presentation only (reconcile with COMBAT_RESULT, log mismatches)
- Combat playback = pure reducer over the event log; the Pixi layer renders reducer output only (never MatchState, never re-runs game logic)
- Bots never persist (no accounts/profiles, accountId null in match_players); all persistence writes go through the `Repository` interface

## Workspace layout

```
packages/
  data/     — JSON content + typed loader, version 0.1.0
  sim/      — pure deterministic combat engine
  rules/    — pure match state machine (no I/O)
  protocol/ — shared WS message types, envelope, encode/decode, validators (zero runtime deps)
  server/   — Node + ws authoritative server (matchmaker, rooms, sessions)
  client/   — Vite + TypeScript + PixiJS v8 web client
  balance/  — headless batch sweeps over the pure sim; CLI writes the report (only I/O-permitted package)
```

## Commands

```bash
npm install          # install all workspace deps (Node >= 20)
npm test             # typecheck (via pretest) + all tests via vitest
npm run typecheck    # tsc --build (must exit 0; npm test runs it automatically via pretest)
npm run dev          # start Vite dev server for the client
npm run server       # start the authoritative WS server (default port 3001)
npm run balance      # run the comp sweep, write balance-report.md + balance-report.json (flags: --seeds N, --out DIR)
docker compose up -d # local postgres:16 (then export DATABASE_URL, see below)
```

Env vars (server): `PORT` (default 3001), `DATABASE_URL` (postgres; unset → in-memory repo), `AUTH_SECRET` (HMAC secret for auth tokens; dev default if unset). With docker compose: `DATABASE_URL=postgres://autobattler:autobattler@localhost:5432/autobattler`. The postgres repo contract tests run only when `DATABASE_URL` is set (skipped + logged otherwise).

## packages/sim internals

- `prng.ts` — mulberry32 seeded PRNG; all sim randomness must use this
- `fixed.ts` — integer fixed-point helpers; scale 1000
- `hex.ts` — axial hex grid 7×8, neighbors, distance, A*
- `types.ts` — UnitDef, TraitDef, ItemDef, UnitInstance (has tier+star), BoardState, CombatEvent, CombatResult
- `engine.ts` — `simulateCombat(boardA, boardB, seed, data) → CombatResult`
  - Applies star multipliers (1.8x at 2-star, 3.24x at 3-star) at combat start
  - Applies trait breakpoint bonuses per team at combat start
  - Applies item stat bundles per unit at combat start
  - Fixed timestep 20 ticks/s, max 1200 ticks then overtime (ramping true damage)
  - Per-tick order: status effects (burn DoT + buff/shield expiry) → mana/cast → movement → attacks → death cleanup
  - PvE mobs reuse `UnitInstance`/the engine unchanged (no new behaviors beyond the supported set): a player board (side 0) fights a mob board (side 1). Mob defIds are absent from `data.units`, so `applyTraits` never counts them — mobs never contribute to (or receive) player trait bonuses
  - Targeting: nearest enemy, tiebreak lowest uid; start-of-combat-stealth units are untargetable until their stealth tick elapses
  - Abilities at full mana (`UnitInstance.ability.effect`): `magic_damage` (single-target), `burn` (magic dmg + DoT), `shield` (self absorb), `buff` (self stat buff, reverted on expiry); `stealth` resolves at combat start, not on cast. Item passives reuse the same primitives: on-hit `burn`, start-of-combat `shield`. Damage routes through any shield before HP.
  - Emits ordered CombatEvent log that fully describes combat without re-running game logic: init (per-unit snapshot post star/item/trait), move (from/to), attack (dmg, crit), cast, mana/hp (absolute values, emitted only on change, hp clamped at 0), death, overtime_start, end (winnerSide, survivingUids)
  - survivingUnits in CombatResult carries tier+star for damage calculation

## packages/data content (v1)

- 50 units across tiers 1-5 (13/13/12/8/4). Each has one `origin` + 1-2 `classes` (flattened into `traits`), role-derived stats, and an `ability {name, manaCost=mana, effect}` from the engine-supported set
- 22 traits: 12 origins + 10 classes, each tagged `kind`; breakpoints at 2/4/6 (or 2/4, or 2) derived so every top breakpoint is reachable by unit count; `knight` keeps its armor curve (+200/+500/+800)
- 45 items: 9 stat-only components (incl. iron_sword/chain_vest/mana_crystal) + 36 completed items, one per distinct unordered component pair (`recipe: [a,b]`); a completed item is a stat bundle + at most one passive (`burn` on-hit or `shield` start-of-combat). Rules equip any item id directly; recipe combination is data-only (see design-notes.md)
- economy.json: pool counts by tier, shop odds by level (tiers 4-5 nonzero from mid levels), xp thresholds, streak table, income constants, `pveBaseGold` (5, flat gold every alive player gets on a PvE round), damage constants, MMR constants (mmrStart 1000, mmrK 40, mmrEloDivisor 400)
- mobs.json: PvE creep defs (`{id,name,tier,isMob,stats,traits:[],optional ability}` — abilities limited to the engine-supported set) + per-PvE-round stage layouts (`stages[].units` = `{mobId, slot, star}` placed on the enemy half, scaled by which mobs + star). Loader exports `MobsData` on `gameData.mobs`; mobs are never pooled
- loot.json: rarity tables (common/uncommon/rare/legendary), each a weighted list of `{kind:"gold",amount}` / `{kind:"component",id}` / `{kind:"item",id}` entries, plus `roundDrops` (per PvE round → list of `{rarity,count}` orbs). Loader exports `LootData` on `gameData.loot`. Loot is never pooled and is seeded-deterministic
- loader also exports pure `recipeResult(aId, bId, items?)` → completed-item id for an unordered component pair, else null (the recipe lookup; reuses the 36 existing recipes)
- `ranks.json`: ordered rank bands (Bronze/Silver/Gold/Platinum/Diamond/Master) by ascending `minMmr`; loader exports `RANK_BANDS` + pure `mmrToRank(mmr)` (highest band whose minMmr ≤ mmr, boundaries inclusive on min)
- `design-notes.md`: intent + `// future:` notes for deferred behaviors the engine can't yet execute (kept out of JSON)
- loader exports `DATA_VERSION` (recorded on every persisted match)

## packages/balance internals

- Headless, seeded, pure except the CLI entry (the only I/O-permitted code in the repo)
- `runner.ts` — `runMatchup(boardA, boardB, seeds, data)`: N seeded combats → win rate, avg length, overtime rate, avg survivors
- `compositions.ts` — representative comp archetypes as data (defId+star), `buildBoard` places them deterministically, `activeTraits` lists hit breakpoints
- `sweep.ts` — `runSweep(data, seeds, comps?)`: round-robin every comp vs every other (both orientations) → per-comp win matrix + overall win rate, appearance-weighted per-unit win rate, per-trait win rate, avg game length, overtime rate
- `report.ts` — pure markdown renderer; `cli.ts` (`npm run balance`) runs the sweep and writes `balance-report.md` + `balance-report.json`

## packages/rules internals

- `state.ts` — MatchState, PlayerState (`items: string[]` = the unequipped item inventory: loose components + completed items, plus the accumulated per-match stats `roundWins`/`roundLosses`/`totalDamageTaken`/`totalDamageDealt` — combat rounds only; bye/PvE never count as a W/L or accrue damage), ShopSlot, Phase; MatchState also carries `lastLootOrbs` (Map player id → the round's already-decided `LootOrb[]`, populated by `runPveRound`, empty on PvP/bye) so the canonical client can animate the reveal without re-deriving loot, and `lastRoundResult` (Map player id → `RoundResult {status: won|lost|bye|pve, damageTaken, damageDealt}`, rebuilt fresh each round) so the resolution screen shows the round's real outcome + HP swing without the renderer computing anything
- `pool.ts` — pool counts by tier (29/22/18/12/10), draw/return without replacement
- `shop.ts` — roll 5 slots using shopOdds table; reroll returns current shop to pool
- `economy.ts` — calcIncome (base 5 + interest + streak), levelForXp
- `commands.ts` — applyCommand: BUY, SELL, REROLL, BUY_XP, MOVE, EQUIP, UNEQUIP, COMBINE_ITEMS; validates legality, returns CommandResult (never throws); auto-merge 3 copies → 2-star, cascade 3x 2-star → 3-star. Item system: max 3 items/unit (`MAX_ITEMS_PER_UNIT`); EQUIP moves an inventory item onto a unit (cap-checked) but **auto-combines in place** if the unit already holds a component that completes a recipe with it (net slots unchanged, so it's allowed even at the cap); UNEQUIP returns a unit item to inventory; COMBINE_ITEMS fuses two loose inventory components into the completed item. Typed errors `ITEM_SLOTS_FULL`/`NO_RECIPE`/`ITEM_NOT_FOUND`. Re-exports the pure `recipeResult` from data
- `loot.ts` — pure seeded loot: `generateLoot(round, prng, data)` builds the round's orbs from `loot.roundDrops` and weighted-resolves each from its rarity table (`LootOrb {rarity, reward}`); `applyLootOrb(player, orb)` folds gold into gold and item/component ids into the inventory
- `rounds.ts` — buildPairings (avoids repeat opponents, ghost on odd count), runCombatPhase, distributeIncome; `runPveRound` (on `pveRounds`): builds the stage's mob board once (`buildMobBoard`/`pveStageForRound`, uids from the match namespace, not pooled), runs each alive player's board vs the shared mob board via the sim (deterministic per-player seed from the round seed), deals NO player HP damage / no streak change, then awards `pveBaseGold` + seeded loot (gold→gold, items→inventory). Keeps `lastPairings = []` (no PvP pairing) while storing per-player `lastCombatResults`/`lastOpponentBoards` (the mob board) and `lastLootOrbs` (the already-decided orbs, for the client reveal); the PvP path clears `lastLootOrbs` so a non-PvE round never shows stale orbs. **Per-round/match stats**: `runCombatPhase` defaults every alive player's `lastRoundResult` to a bye (0/0), then each pairing's `recordRoundWin`/`recordRoundLoss` overwrites the winner (`status: won`, `damageDealt`) and loser (`status: lost`, `damageTaken`) — `damageDealt` is the SAME surviving-unit term (`calcPlayerDamage`) used to subtract the loser's HP, so winner.damageDealt === loser.damageTaken; a draw and a ghost-source (unpaired) real player keep the bye 0/0 (no W/L). The same calls bump the PlayerState accumulators (`roundWins`/`roundLosses`/`totalDamageDealt`/`totalDamageTaken`). `runPveRound` sets every alive player to `status: pve`, 0/0 (no W/L, no damage). Pure (no I/O), determinism unaffected
- `match.ts` — createMatch(seed, data), advancePhase, runMatchToEnd
- `ai.ts` — applyAiCommands(state, playerId, prng, data): seeded bot policy for planning phase

## packages/protocol internals

- Zero runtime deps; pure TypeScript types + helpers
- `messages.ts` — C2SMessage union (QUEUE_JOIN (carries `authToken`), QUEUE_LEAVE, CMD, READY, PING, RECONNECT) and S2CMessage union (QUEUE_STATUS, MATCH_FOUND, STATE_SNAPSHOT, STATE_DELTA, PHASE_CHANGE, COMBAT_START, COMBAT_RESULT, LOOT (private per-seat PvE orbs for a round), ROUND_RESULT (private per-seat outcome of the just-finished round), MATCH_END (placements + per-seat `mmr` before/after + per-seat `names` + per-seat `stats`), ERROR, PONG). `ROUND_RESULT {round, result: RoundResultWire {status: won|lost|bye|pve, damageTaken, damageDealt}}` is the private per-seat resolution payload; `MATCH_END.stats?: Record<seat, MatchStats {roundWins, roundLosses, totalDamageTaken, totalDamageDealt}>` carries the accumulated per-seat match stats next to placements/mmr/names. Public player fields in STATE_SNAPSHOT/STATE_DELTA carry `name` (names are public via the leaderboard); PvE loot and round results are private per-seat (sent only to its own human, like gold/shop). Loot wire types (`LootOrbWire`/`LootRewardWire`/`LOOT_RARITIES`) plus `RoundResultWire`/`ROUND_RESULT_STATUSES`/`MatchStats` are declared locally (zero runtime deps), structurally mirroring rules' `LootOrb`/`RoundResult`/PlayerState accumulators
- `envelope.ts` — `{ v, t, p }` envelope; `encode(S2CMessage) → string`; `decodeC2S(raw) → C2SMessage | null` with full runtime validation; `decodeS2C` for client side (rejects LOOT with malformed orbs / ROUND_RESULT with a bad status or missing damage / MATCH_END with malformed `names` or `stats`). Exported field validators `validateLootOrbs`/`validateNameMap`/`validateRoundResult`/`validateMatchStatsMap`/`isValidName`

## packages/server internals

- Single-process Node server on `PORT` (default 3001): HTTP API + ws on the same port
- `session.ts` — connection registry, seat tokens, per-connection rate limit (20 CMD/s), AFK tracking, accountId + profile `name` set on authed QUEUE_JOIN
- `matchmaker.ts` — FIFO queue; 8 players OR 10s timeout with ≥1 human; backfills remaining seats with AI bots
- `room.ts` — owns MatchState; authoritative 30s planning timer; applies validated commands via applyCommand; broadcasts STATE_DELTA on each accepted command; at combat start runs sim server-side and broadcasts COMBAT_START + COMBAT_RESULT; on a PvE round's resolution sends each human its OWN seeded loot orbs privately (LOOT, from `state.lastLootOrbs`, before the RESOLUTION PHASE_CHANGE), and on every round's resolution sends each human its OWN ROUND_RESULT privately (from `state.lastRoundResult`, also before the PHASE_CHANGE — same per-seat-private pattern as LOOT); READY from all human seats skips the 30s wait; at match end persists via recorder before broadcasting MATCH_END (so payload MMR always matches repo state) with per-seat `stats` (round W/L + total damage taken/dealt, read off final PlayerState — payload-only, not persisted); captures `seatAccounts` + `seatNames` at creation (human = profile name from the session, bots = generated "Bot N"); public player serialization carries `name`, MATCH_END carries the per-seat `names` + `stats` maps
- `index.ts` — HTTP + WS server, heartbeat via ws.ping every 5s, reconnect via token restores seat; QUEUE_JOIN without a valid `authToken` → typed ERROR `UNAUTHENTICATED`
- `auth.ts` — HMAC-signed opaque tokens (`signToken`/`verifyToken`), secret from `AUTH_SECRET` (dev default)
- `http.ts` — minimal HTTP API on the WS port: `POST /auth/guest {deviceId, name?}` → `{accountId, token, profile}` (idempotent per deviceId); `GET /leaderboard?n=50`; `GET /profile` and `GET /history?limit=20` (Bearer token); `PATCH /profile {name}` (Bearer token; `validateName` enforces length 2-16 + charset `[A-Za-z0-9 _-]`, rejects with typed `INVALID_NAME`, persists via `repo.updateProfile`, returns updated profile; bots unaffected); CORS-enabled (GET/POST/PATCH)
- `db/` — `repo.ts` (`Repository` interface: createGuest, findByToken, get/updateProfile, recordMatch, leaderboard, matchHistory), `memory.ts` (default/dev/tests), `postgres.ts` + `schema.sql` (used when `DATABASE_URL` set; migrations = idempotent schema.sql; recordMatch inserts match + match_players and applies profile MMR updates in one transaction); contract test suite runs against both (pg only with DATABASE_URL)
- `mmr.ts` — Elo for 8-player FFA: expected = Elo expectation vs lobby's average MMR excluding self; actual = (8 − placement)/7; delta = round(K·(actual − expected)); K/start/divisor from economy.json; bots count at mmrStart for the lobby average but are never persisted
- `recorder.ts` — `recordMatchResult(repo, matchId, seats)`: fetches profiles, computes deltas, writes match + MMR through the repo, returns per-seat `{before, after}` for the MATCH_END payload

## packages/client internals

- Vite + TypeScript + PixiJS v8; design resolution 390×844, scale-to-fit
- **Orientation-aware layout (`layout.ts`, pure + unit-tested)**: `resolveLayout({viewportW,viewportH,safe?})` picks orientation by aspect (landscape when w/h ≥ 1.1, else portrait), returning a `MatchLayout {orientation, designW, designH, scale, canvasOffsetX/Y, regions}`. `regions: MatchRegions` are named design-space rects (`board, opponentRail, traitRail, shop, bench, hud, itemBar, readyButton, statusRow, sellControl`); **portrait regions faithfully mirror the prior hardcoded match.ts positions** so portrait stays pixel-identical. `centeredModal(layout, w, h, pad=20)` centers + clamps a modal into the design bounds; `landscapeBenchSlotCenter(i, benchRect)`/`landscapeBenchSlotAt(px,py,benchRect)` are the 3×3 landscape bench hit math. **Polish-pass pure helpers (unit-tested):** `planningRegionAt(px,py,layout,boardSlot,benchSlot,itemCount,itemSlot,itemGap)` → the named planning region a pointer is in (`board/bench/sell/itemBar/hud/shop/readyButton`|null, forgiving sell ±6px); `opponentRailTile(seatIdx,cols,rows,rail)` → one rail tile's `{col,row,tileX/Y,cx,cy,tileW/H}` (used by `renderHud`); `benchLayout.ts` adds `portraitBenchGeom(bench)` (region-based 9-slot geometry) and `benchGeom` gained an optional `regionH` param so portrait slot height tracks the bench region (now 36px). Portrait lower-column regions use a uniform `SECTION_GAP=8` (consistent rhythm; board/trait-strip positions unchanged) and the portrait `itemBar` is 2 rows tall (68px); landscape opponent-rail tiles are 36px tall. `main.ts` calls `resolveLayout` on `resize` AND `orientationchange` (`setTimeout(resize,100)` — iOS reports stale dims at the event), reads safe-area insets from a hidden `#safe-probe` element's computed `env(safe-area-inset-*)` padding, `app.renderer.resize(designW,designH)` (Pixi v8 dynamic) + CSS-sizes/positions the canvas, holds an `activeLayout` it passes into `MatchScene` and feeds via `scene.onLayoutChange` on every resize. A future Capacitor `ScreenOrientation.lock({type:"landscape-primary"})` (gated on `Capacitor.isNativePlatform()`) is marked in `main.ts` but NOT implemented (web stays orientation-responsive)
- **Landscape match layout**: a three-column design (844×390) — board centered/enlarged in the middle, the trait rail stacked **vertically** down the left, the 9-slot bench as a **3×3 grid** + sell + item bar in the left column, and the opponent rail (4×2 grid) + HUD cluster + shop (5 wide cards) + Ready in the right thumb zone. `match.ts` renders entirely from `this.layout.regions` (no module-level position constants); board hex offsets derive from `regions.board`, every overlay (scout/resolution/match-over/inspect panels) centers via `centeredModal` and compresses for the short 390px height, and `onLayoutChange` re-renders the current phase. Portrait is the preserved fallback (identical to before)
- Boot flow: `bootAuth` (guest auth, tolerant of an offline server → null) → Main Menu (not straight to mode select). Practice works offline; Online/Profile/Leaderboard need a reachable server. Meta screens get safe-area padding + a landscape `@media` block (wider/scrollable, compressed spacing, pause button moved right)
- `ui/` — DOM/CSS meta-screen layer over the Pixi canvas (`#ui-root` for menus, `#match-overlay` for the in-match pause button/coachmarks; canvas stays Pixi). **Visual-overhaul stage 4 themes every meta screen to the in-game art direction** (one shared CSS component set: panel surfaces `panelBg`/`chipBorder`, gold accents, themed buttons/cards/inputs/sliders/toggles — no default HTML chrome). `app.ts` = `UiApp` screen manager (MainMenu wordmark + primary Play, Play→Practice/Online themed cards, Profile, Leaderboard, HowToPlay, Settings) with back-nav, the themed in-match pause panel (master volume, default speed, Leave Match), and the coachmark trigger. Rank shown as a tier-colored `ui-rank-badge` (color from `rankCssVar(band.id)`, reusing `RANK_BANDS`); Leaderboard highlights the current player (`.me` by accountId); Profile shows placement-distribution bars + colored MMR deltas; How-to-Play pages reuse the diamond + tier/trait-color motif (`ui-diamond`/`ui-tier-legend`/`ui-trait-legend`). `styles.ts` injects one stylesheet whose colors are CSS custom properties generated from theme.ts (no hex literals); `content.ts` = How-to-Play pages; `coachmarks.ts` = first-match highlight overlay; `dom.ts` = element helpers. Stage 4 is pure visual/CSS — no navigation/logic change
- **Theme-as-CSS-vars single source (DOM + canvas)**: `theme.ts` is the only palette. The same numeric colors the Pixi layer uses are exported to the DOM as `:root` custom properties (`applyThemeVars`/`themeCssVars`); DOM/CSS reference them via `cssVar(key)`, never hex literals (theme test forbids `0x` outside theme.ts). Visual-overhaul stage-1 constants also live here: tier colors `tier1..tier5` (read by `tierColor`), token bars `hpGreen`/`hpLow`/`manaBlue`, hex board `enemyHex`/`myHex`/`boardBg`/`boardBorder`, and token `tokenBg`/`starGold`. Visual-overhaul stage-2 in-match chrome constants also live here: `panelBg`/`chipBorder` (chips), `xpPurple` (xp bar), `streakOrange` (streak), reusing `starGold` for gold/cost; plus the stable family-based `TRAIT_COLOR` map read by `traitColor(traitId)` for trait-strip chip hues. Visual-overhaul stage-4 (DOM meta) constants too: `bgPanelRaise`/`accentGold` + per-rank `rankBronze..rankMaster`, with `RANK_COLOR`/`rankColor(id)`/`rankCssVar(id)` mapping ranks.json band ids to badge colors and `traitColorCss(id)` exposing the canvas trait color to DOM motifs. Non-trait HUD glyphs `coin`/`refresh` added to `glyphs.ts` (`drawGlyph`), kept out of `TRAIT_GLYPH`. Stage-5 in-match-UX constants too: `bgInspect`/`bgInspectRow`/`bgScrim` (inspect & trait-detail panels), `bgSellChip`/`bgSellArmed` (sell affordance), `benchEmpty`/`benchEmptyRim`/`benchOccupied` (bench slot states). **Phase-10b (items/loot/PvE) constants too**: item chips `itemComponent`/`itemCompleted`/`itemBorder` + combine feedback `itemCombineOk`/`itemCombineNo` + `itemSlotPip`; loot rarity orb colors `lootCommon`/`lootUncommon`/`lootRare`/`lootLegendary` (+ `lootOrbCore`) with `RARITY_COLOR`/`rarityColor(rarity)` mapping loot.json rarities; PvE presentation `mobTint` (neutral mob ring) / `mobZone` (creep board zone) / `pveLabel`. Non-trait item glyphs `gem`/`component`/`bag` added to `glyphs.ts` (`drawGlyph`), kept out of `TRAIT_GLYPH`. **Polish-pass surface-elevation system**: four canonical depth steps `surfaceBase`/`surfaceRaise`/`surfaceFloat`/`surfaceOver` are the single source for the dark surface stack — the existing `bg*`/`panelBg`/`bgInspect*`/`bgReroll`/`bgXp`/`bgScout`/`bgReadyOff`/`bgPanelRaise`/`tokenBg`/`bgUnit` keys keep their NAMES but their VALUES alias these four steps (call sites unchanged), so page < panels/cards < chips/HUD/rows < modal overlays reads as real depth. Added border variants `borderSubtle` (inactive card/slot edges) + `borderActive`, the drag-state `bgBoardDragOver` (occupied hex swap-hint fill), and a tightened two-gold split: `accentGold` (bright — coin glyph everywhere, primary CTA, wordmark, star pips) vs `textGold` (legible reading gold for HUD/cost numbers); `starGold` is now narrow (star-pip fills only). `chipBorder`/`boardBorder`/`hpBg`/`manaBg`/`lootCommon`/`benchEmpty(Rim)`/`benchOccupied` lifted for 2×-DPR legibility. `glyphs.ts`/`itemIconDraw.ts` share one step-based stroke-weight formula (small marks read with consistent ink; the coin self-stroke gates on size>10)
- **HUD model (`hudModel.ts`)**: pure derivations for the stage-2 chrome (no Pixi, no game logic). `traitStripModel(board, units, traits)` → one chip per trait by unique-defId count vs breakpoints (`{traitId,name,count,activeBreakpoint,nextBreakpoint,color}`, sorted active-first then count desc); `xpProgress(xp, level, levelXpThresholds)` → fill fraction within the current level (maxed at top level). Both unit-tested
- **Inspect/detail models (stage-5 in-match UX, pure + unit-tested, mirror the hudModel pattern)**: `inspectModel.ts` (`inspectModel(defId, instance|null, data)` → identity + origin/classes + cost/tier + full stat block + readable `abilityDescription`; reads units.json, uses the live UnitInstance's hp/mana/star when owned, base stats for a shop preview); `traitDetailModel.ts` (`traitDetailModel(traitId, count, data)` → per-breakpoint `{count, effect, active, reached}` from traits.json + `breakpointEffect` stat-line formatter); `sellValue.ts` (`sellValue(unit, data)` — pure mirror of the rules SELL refund: tier × copies-per-star × multiplier); `benchLayout.ts` (`benchGeom(designW, benchY)` + `benchSlotAtX` — pure bench/sell hit-target math); `combatLayout.ts` (combat-layer z-index constants; regression guard so the header outranks the tiles)
- **Item / loot models (phase 10b, pure + unit-tested, mirror the hudModel/inspectModel pattern)**: `itemModel.ts` (`itemModel(id, data)` → name + component-vs-completed flag + tint + fixed-point-safe stat bundle + `passiveDescription`; `inventoryModel(items, data)` preserves the rules array order so an index maps straight back to a command target; `equippedSlots(unit, max, data)` reports slot occupancy/free/full vs the cap); `combinePreview.ts` (`combinePreview(aId,aIndex,bId,bIndex,data)` wraps the data `recipeResult` → completed-item model or typed `same-entry`/`no-recipe`; `equipPreview(itemId, heldIds, data)` mirrors the rules EQUIP auto-combine → `combine`/`add`); `lootReveal.ts` (`lootRevealModel(orbs, data)` orders the seeded `LootOrb[]` into deterministic `RevealStep`s — ascending rarity, stable within a rarity, gold→gold-counter / item·component→inventory destination — plus `totalGold`/`itemCount` for the reduced-motion summary). `inspectModel` now also carries the unit's equipped `items` (ItemModels).
- **Items in inspect (`inspectPanel.ts`)**: `renderUnitInspect` draws an equipped-items row (glyph + name chips) under the stat grid (panel grows to fit); `renderItemDetail(layer, itemModel, onClose)` is the modal item-info panel (item disc/glyph + name/kind + stat bundle rows + passive line), opened by tapping a loose inventory item — reuses the same `inspectLayer` modal pattern.
- **Item inventory bar + drag (phase 10b, `scenes/match.ts`)**: a planning-phase `itemLayer` below the Ready button renders the player's `items` as chips (components vs completed tinted + glyph, via `inventoryModel`). Drag a chip onto a board/bench unit → EQUIP (auto-combine surfaced with an `itemCombineOk` flourish on the unit, derived from the unit's slot-count change — no new game logic); drag onto another inventory item → COMBINE_ITEMS with a live combine-preview hint (`combinePreview`; `itemCombineNo` ring + NO_RECIPE toast when no recipe, no command sent); tap a chip → `renderItemDetail`. Equipped-item dots render on board/bench `UnitToken`s (component vs completed tint). All actions go through `driver.playerCommand`; rejections use the existing `showToast` (e.g. ITEM_SLOTS_FULL).
- **Loot orb reveal (phase 10b, `scenes/match.ts` `lootLayer`)**: on a PvE round's RESOLUTION, `startLootReveal` plays `lootRevealModel(driver.getMyLootOrbs())` — orbs pop in by ascending rarity (distinct color + shape), crack open to reveal their already-decided content, then fly to the gold counter / item bar; reduced-motion shows an instant summary chip instead. Presentation only (rules decided the orbs); torn down on every phase change / match-over / destroy (tracked tickers+timers).
- **PvE round presentation (phase 10b, `scenes/match.ts`)**: a PvE round reads as PvE not a bye — the combat header shows `PvE · <stage name> · Creeps` (`pveLabel`), the enemy zone uses the `mobZone` tint, and the mob board plays back through the same `CombatPlayer`/`CombatView` (the driver exposes the PvE combat result + mob board even though there's no pairing). Mob tokens auto-pick the neutral `mobTint` ring (a defId absent from `data.units` is a mob). Driver additions `isPveRound()` / `getPveStageName()` / `getMyLootOrbs()` (LocalDriver reads `lastCombatResults`/`lastOpponentBoards`/`lastLootOrbs` keyed by player id for PvE; NetDriver populates `lastLootOrbs` from the private per-seat LOOT message so the loot reveal animates online exactly as in Practice — Practice/Online parity for the item/mob/loot system). Display names: both drivers carry a per-seat `name` on the client player view (NetDriver from the public snapshot/delta `name` + the MATCH_END `names`; LocalDriver names seat 0 with the profile name and the rest "Bot N") — `seatName` resolves the real name on the opponent rail + match-over, falling back to "Player N".
- **Stat display formatter (`statFormat.ts`, pure + unit-tested)**: single source for turning a stored stat value into its display string, so raw fixed-point never leaks to the UI. The sim stores most stats as their real value (hp/ad/armor/mr/range/mana/abilityDamage) but attack speed is fixed-point (scale 1000, e.g. 750 = 0.75). `formatStat(stat, value)` formats an absolute stat (`as` ÷1000 to 2 decimals, the rest raw); `formatStatDelta(stat, value)` formats a signed grant (trait breakpoint / buff — `as` deltas are fixed-point too, e.g. +280 → "+0.28"). Used by `inspectModel` (stat block + buff `abilityDescription`) and `traitDetailModel.breakpointEffect`. The scale constant mirrors `sim/src/fixed.ts` locally rather than widening sim's narrow exports.
- **Inspect panels (`inspectPanel.ts`, Pixi — same world as the scout overlay)**: `renderUnitInspect`/`renderTraitDetail` render the pure models into a modal `inspectLayer` (scrim-dismiss + ✕): unit panel = token + name + tier·cost, trait diamond+glyph chips, ability block (name/cost/wrapped description), 2-col stat grid; trait panel = diamond + name/kind/count + per-breakpoint rows marking reached/active (shape+arrow, not color-only). Opened by long-press on any unit (shop/bench/board) or tap on a trait-strip chip; no game logic, dismissable, torn down on every phase change
- **UnitToken (`unitToken.ts`)**: one reusable Pixi component drawn on the board, the bench, and in combat — disc (`tokenBg`) + 2.5px tier-color ring (a defId absent from `data.units` is a PvE mob → neutral `mobTint` ring), class glyph (or drop-in art) centered, gold star pips above, optional HP/mana bars below (bench tokens pass no bars), and optional equipped-item dots along the disc base (component vs completed tinted) when the unit holds items. Built with the Pixi v8 path API (`circle/poly/rect` + `fill`/`stroke`). The hex board renders flat-top tiles on a `boardBg` panel (enemy zone `enemyHex`, player zone `myHex`); while dragging a unit the player's own hexes outline-highlight as valid drop targets
- **Class glyphs (`glyphs.ts`)**: Tabler's webfont can't render in canvas/WebGL, so every origin + class trait maps to a procedural vector glyph in `TRAIT_GLYPH` (completeness is test-enforced); `drawGlyph` paints it. A unit shows its primary class glyph (`glyphForTraits`)
- **Unit art drop-in slot (`sprites.ts` + `public/units/`)**: mirrors the audio music slot — drop `public/units/<unitId>.png` and the token renders it clipped inside the ring instead of the glyph; absent → glyph fallback, no-op clean. Pure `resolveUnitTexture(unitId, lookup)` (path→texture|null) is the tested unit; `requestUnitArt` lazily loads + caches, `onUnitArtReady` lets the static planning board repaint when art lands. See `public/units/README.md` for naming/size
- **Item icon system (`itemIcon.ts` pure + `itemIconDraw.ts` Pixi)**: every item renders a distinct procedural icon instead of one generic glyph. `itemIcon.ts` is the pure, unit-tested resolution: `COMPONENT_EMBLEM` maps each of the 9 base components to a distinct `ItemEmblem` archetype (blade/vest/crystal/bow/cloak/belt/rod/glove/flask, derived from its dominant stat); `itemIcon(id, data)` returns a `component` icon (one emblem) or, for a completed item, a `completed` icon carrying the two emblems + source component ids derived from its `recipe` (cross-checked by `recipeResult`, no game logic duplicated) — every real item resolves, never the generic glyph (test-enforced). `itemIconDraw.ts` paints with the Pixi v8 path API like `glyphs.ts`: `drawEmblem` per archetype, components drawn raw/unframed in neutral `itemEmblem` ink, completed items composing their two emblems (`itemEmblem`/`itemEmblemAlt`) inside a gilded `itemFrame` rounded frame with corner gilding + an `itemShine` glint; the animated shine sweep is reduced-motion gated (static frame/glint stays). Rarity tint (`rarityColor`) is consistent across all surfaces. Applied everywhere items appear: inventory bar chips + loot-orb reveal contents (`scenes/match.ts`), equipped-item icons on board/bench `UnitToken`s (dots upgraded to tiny icons), inspect equipped-items rows + the item-detail modal (`inspectPanel.ts`). Theme constants (`itemEmblem`/`itemEmblemAlt`/`itemFrame`/`itemShine`) live in `theme.ts` (no hex outside it)
- **Item art drop-in slot (`itemIcon.ts`/`itemIconDraw.ts` + `public/items/`)**: mirrors the unit art slot — drop `public/items/<itemId>.png` (the items.json id, e.g. `iron_sword.png` or the underscore-joined `iron_sword__sorcerer_rod.png`) and the icon renders it clipped to a disc instead of the procedural emblem; absent → emblem fallback, no-op clean. Pure `resolveItemTexture(itemId, lookup)` (path→texture|null) + `itemArtPath` are the tested units; `requestItemArt` lazily loads + caches, `onItemArtReady` lets the static planning board/inventory repaint when art lands. See `public/items/README.md` for naming/size
- `settings.ts` — `SettingsStore` persists client prefs in localStorage (master/sfx/music volume, mute, `musicEnabled` on/off, default combat speed 1x/2x, reduced motion). Gameplay-affecting prefs read by the match scene (default speed, reduced motion → CombatView skips non-essential tweens) and audio (volumes/mute/music toggle). Player name is NOT a setting — changed via `PATCH /profile` (`patchName`)
- **Audio (`audio/`)** — Web Audio engine; master/sfx/music gain buses bound to Settings volumes, mute zeroes master, the music toggle gates the music bus (pure `computeGain(master, channel, muted)` mirrors the node graph). Pure data/logic modules are unit-tested; `manager.ts` wraps them around real nodes:
  - `sfx.ts` (pure palette): the full designed SFX set as data — layered synth voices (ADSR + multi-osc + filtered noise + per-spec reverb send + per-trigger pitch/velocity jitter), one A-minor key/timbre family. Covers UI (tap/buy/sell/reroll/levelUp/error), combat (attack=melee contact/projectile/impact/crit/death + per-ability-kind casts castMagic/castBurn/castShield/castBuff/castStealth), and economy/feedback (goldGain/starUp/roundStart/roundWin/roundLoss/elimination). `EVENT_SFX` is the total event→sound coverage map (test-enforced complete); `castSfx(kind)` and `SFX_NAMES`/`SFX_SPECS` are the lookups
  - `music.ts` (generative): original loopable music composed from scratch (no copyrighted melodies) — a moving multi-chord progression rendered as a high-passed pad stack + a root bass line + a gentle arpeggio + a short seeded lead motif + light percussion per `MOODS` (menu calm / planning warm-mid / combat tense-rhythmic) in an A-minor/dorian family, scheduled by a Web Audio lookahead scheduler (`dueSteps`) that evolves slowly. The musical logic is extracted as pure, unit-tested helpers: `PROGRESSIONS`/`progressionFor(mood)`/`triad` (a distinct 4-bar diatonic progression per state that moves and resolves at the loop seam — menu i–VI–III–VII, planning i–iv–VI–V, combat i–VII–VI–v); `generateMotif(seed,len)` (deterministic mulberry32 motif as scale-degree offsets with stepwise motion + occasional skips/rests, one stable `MOTIF_SEED` per mood) re-varied each loop by `varyMotif(motif,pass)` (deterministic per-pass ornament/octave-lift/rest toggle; `pass` 0 states the theme plainly, so the lead repeats with slight change, not identically); `voiceLead(prevMidi,chordDegrees,rootMidi,scale)` (picks each chord tone's nearest octave to the previous voicing so motion is shared/stepwise, not parallel leaps); and the `MIX` gain table (per-layer multipliers in (0,1], lead above pads, percussion never dominant — pads high-passed and layers band-separated to avoid mud, all trimmed low to underscore). Also `midiToFreq`/`degreeToMidi`/`dueSteps`/`MOODS`. `GenerativeMusic.setMood` re-parameterizes (incl. re-seeding the motif and re-anchoring voice-leading) without restarting; `stop` saves CPU
  - `director.ts` (pure): maps phase/scene → `MusicState` (`phaseToMusicState`: PLANNING→planning, COMBAT/RESOLUTION→combat; match-over→results), `stateToMood`, `resolveMusicSource(state, hasFile)` (a dropped-in file always wins, else generative), file-slot paths (`musicFilePaths`/`sfxFilePaths`), equal-power `crossfadeGains`, and the sticky autoplay-unlock state machine (`nextUnlockState`/`becameUnlocked`)
  - `manager.ts`: `setMusicState(state)` resolves file-vs-generative and crossfades on transitions; `play(name, delaySec?)` synthesizes a spec (or plays a per-event override sample if dropped in). Combat audio stays a **consumer of the combat fx/effect stream** (`handleCombatFx(frame.fx)` from `combat/player.ts`), never game logic: melee `contact`→attack, `impact`/crit→impact/crit, `projectile`→fire, `abilityHit.effect`→per-kind cast, `dissolve`→death — so it still tracks the visuals under reduced motion (heavy motion fx dropped, impact/abilityHit/dissolve remain). The match scene drives music states + economy/feedback cues by consuming phase/scene like the renderer (no game logic). `resume()` (first user gesture) resumes the suspended context and (re)starts the current state so menu music begins on first interaction; muting/zero-volume/music-off stop generative nodes
  - **File slots**: per-state music at `public/audio/music/<state>.(mp3|ogg)` (state ∈ menu/planning/combat/results) and optional per-event SFX overrides at `public/audio/sfx/<name>.(mp3|ogg)`; generative/procedural is the fallback, a dropped-in file always wins, absent files no-op cleanly. See `public/audio/README.md`
- `onboarding.ts` — first-Practice-match coachmark gating (localStorage `ab.coachmarksSeen`, shown once/skippable) + step data; full scripted tutorial match is out of scope (see design-notes.md)
- `auth.ts` — guest auth: deviceId (crypto.randomUUID) + token persisted in localStorage; registers as "Guest" via POST /auth/guest, reuses/validates the stored token via GET /profile; helpers `fetchProfile`/`fetchHistory`/`patchName`/`fetchLeaderboard`; NetDriver sends the token in QUEUE_JOIN
- `mmrToRank(mmr)` (from `@autobattler/data`, thresholds in `ranks.json`) maps MMR → rank band; used by Profile + Leaderboard rows
- Post-match overlay shows placement + MMR delta (from MATCH_END `mmr`) + the local player's match summary (Record W–L, total damage dealt/taken from MATCH_END `stats`, in the content budget so nothing collides) and a Main Menu button on a themed rounded panel (see `scenes/match.ts` above); leaving an online match disconnects (driver.dispose → NetClient.stop), forfeiting to last via the server's AFK path (no SURRENDER message exists)
- `driver.ts` — `IDriver` interface + `LocalDriver`: wraps match.ts with 30s planning timer, skippable via ready(); AI commands applied at phase end; emits DriverEvent stream (`match_over` carries optional per-seat `stats`). Stat accessors `getMyRoundResult()` (this seat's `RoundResult` for the resolution screen) + `getMatchStats()` (per-seat `MatchStats` map) — LocalDriver reads both straight off MatchState (`lastRoundResult` + the PlayerState accumulators)
- `netDriver.ts` — `NetDriver implements IDriver`: drives scene from server messages; runs local sim from received seed for rendering; reconciles with COMBAT_RESULT (logs mismatch); caches the private per-seat ROUND_RESULT into `lastRoundResult`/`getMyRoundResult()` (cleared each PHASE_CHANGE to PLANNING, like loot) and the MATCH_END `stats` into `getMatchStats()` — Practice/Online parity for the stats slice
- `net.ts` — `NetClient`: WS wrapper with reconnect backoff + token; PING/PONG RTT measurement for clock offset
- `scenes/match.ts` — single screen, stage-2 chrome (all Pixi, rendered from the MatchState snapshot): status row (stage chip + planning timer m:ss, tinting `hpLow` under 5s; ☰ pause stays the DOM `#match-overlay` hook), opponent rail (8 seat tiles: avatar + HP bar, **seat number + level `L#`** on the avatar, current opponent gold-outlined, eliminated dimmed; alive opponents during planning carry a visible **eye-badge scout affordance** and tap-to-scout — display names + opponent gold are deliberately NOT shown here), hex board (player 4 rows + opponent 4 rows), trait strip (`renderTraitStrip` from `traitStripModel`: diamond+glyph chips, active colored / inactive dimmed `count/next`; tap a chip → trait-detail panel — chips drawn by the shared `drawTraitChip`, reused by the scout overlay), HUD row (`renderControls`: level chip + `xpProgress` bar, gold, streak, reroll + buy-xp buttons), bench (9 full-cell slots with clear occupied/empty states + forgiving hit areas, via `benchGeom`), sell control (lights up + shows the exact gold refund from `sellValue` when a unit is selected/dragged; tap or drag-to-sell), shop cards (5: tier-top-border + portrait disc + name + Origin·Class + cost), full-width Ready button. **Button press feedback** (`pressFeedback`): reroll / buy-xp / ready (and the combat speed/skip buttons) get an instant alpha-dip pressed state on pointerdown + a quick scale pop, but the ACTION fires on **pointerup** (slide-off-to-cancel; consistent with the shop cards + DOM buttons) — the pop is gated behind reduced-motion (instant fallback still visibly presses) and the scale pivot is passed in (no per-press `getLocalBounds`). **Polish pass:** landscape HUD splits into two sub-rows (economy display vs action buttons); the planning→combat cut is bridged by a 120ms layer cross-fade (reduced-motion skips it); the selected board unit gets a `tier3` halo ring + during a unit-drag the valid player hexes glow green (occupied = `bgBoardDragOver` swap tint) while the enemy zone dims to 0.4α; the Ready button reads as a primary CTA (top highlight band + bold); the sell control shows only a dim `dagger` glyph at rest (label/refund only when armed); the landscape left column gets a subtle column-bg; the inspect/trait/item panels scale-in (0.88→1, reduced-motion snaps) and their close buttons carry a 44px hit area. Inspect: long-press any unit (shop/bench/board) → `renderUnitInspect`; modal panels live in `inspectLayer`, torn down on every phase change. **Toast** (`showToast`): flexible width (grows with the text up to a cap, word-wraps when longer), dedupes rapid identical errors (refreshes the existing toast's lifetime instead of stacking), reduced-motion-aware (no fade-in when reduced). **Match-over overlay** + **scout overlay** are themed rounded panels (scrim + `bgInspect` surface + accent rim) consistent with the inspect/resolution panels; the match-over layout budgets 8 placements + the MMR line + the Main Menu button so nothing collides, and the scout overlay renders the opponent's board (long-press a token → `renderUnitInspect`) and traits (`traitStripModel` chips → `renderTraitDetail`) so a scouted board reads identically to your own inspect views. Both overlays resolve a player's display name via `seatName` (falls back to "Player N" — names aren't currently carried in client match data, so the fallback is what renders today)
- `combat/` — event-log playback: `reducer.ts` (pure fold of CombatEvents → positions/hp/mana/alive per uid; `stateAtTick`), `player.ts` (tick→ms clock at ticksPerSec, 1x/2x speed, skip-to-end, lerped MOVE spans, typed fx stream; `toDisplayHex` keeps my units on the bottom rows regardless of pairing side, row flip is involutive), `view.ts` (Pixi layer rendering player frames via the shared UnitToken + the fx stream)
- **Combat VFX/fx stream (stage 3)**: every visual and every combat sound is derived in `player.ts` from the CombatEvent log — playback stays a pure deterministic function of (log, seed). `CombatPlayer(log, ticksPerSec, data, {reducedMotion})` resolves positions from the reducer state at each event tick, ranged-vs-melee from each unit's `range`, and the ability kind from the caster's `ability.effect.kind` (both via the passed `data`). Fx kinds: `projectile` (ranged bolt, travels `fromPos→toPos` over `travelTicks`, view spawns the landing `impact`) / `contact` (melee lunge) / `impact {pos,targetUid,crit}` (hit-spark + target recoil; crit → scale-pop + shake) / `floater` (weighted damage number: crit larger + gold) / `abilityCast {casterPos,effect}` + `abilityHit {targetPos,targetUid,effect}` (per-kind: magic burst / shield bubble / buff aura / burn flicker / stealth fade) / `dissolve {uid,pos}` (death fade + sink/scale + burst) / `overtime`. `view.ts` animates HP down smoothly with a trailing white damage-chip (`unitToken` bars `hpChipFrac`), fills mana smoothly, screen-shakes (capped) on crit/death, and adds an `overtime` board-edge tint beside the banner. **Reduced-motion gating** lives in BOTH layers: `player.ts` downgrades the *emitted* set (drops `projectile`/`contact`/`abilityCast`, emits `impact` directly for ranged) so audio + readability survive; `view.ts` skips all particle spawns + shake and snaps bars near-instantly. Planning-phase juice (`scenes/match.ts`): star-up flourish on a merge (derived from the board/bench star-count change, no new game logic) + buy/sell pops, all reduced-motion-gated. **Perf**: fx objects pool a Graphics free-list, concurrent particles + projectiles are capped (oldest evicted past budget) for smooth full-board mobile playback
- **Renderer-is-dumb invariant**: no game logic in the renderer; render strictly from MatchState snapshots and CombatEvent logs; all actions go through driver.playerCommand → applyCommand
- Planning phase: tap shop card to buy (long-press to inspect); tap unit then tap hex/bench slot to move; tap the sell control (shows refund) or drag onto it to sell; long-press a unit / tap a trait chip to inspect; drag an inventory item onto a unit to equip or onto another item to combine (preview shown) / tap it for its info panel; rejections shown as brief toast
- Combat phase: event-log playback of my pairing with speed/skip HUD buttons (sized to the 44px touch-target min, styled with the shared chip set so they read as part of the HUD); single `COMBAT · vs X` header sits in the clear band below the board panel with an explicit high zIndex so it never renders behind the hex tiles (the sortable combatLayer keeps insertion order at equal zIndex — see `combatLayout.ts`); LocalDriver holds RESOLUTION until the scene calls `combatPlaybackDone()` (capped at 1x duration + buffer); online, playback auto-skips to the end if the server advances first; PvE/bye rounds show a static board
- Resolution: round-result overlay shows this round's outcome detail from `driver.getMyRoundResult()` (win → "Dealt N damage", loss → "−N HP", PvE/bye → "no damage", all decided by rules — the renderer only displays the numbers) above a wide thumb-reachable Continue button + a VISIBLE auto-advance countdown (draining bar + "Auto in Ns" label synced to `resolutionSeconds`, ticker-driven, reduced-motion still shows the seconds); the countdown ticker is cleared alongside the timer in `clearResolutionTimer` (and on phase change / match-over / destroy) so the overlay always tears down cleanly

## Agent routing policy

For any non-trivial task, delegate to the most specific matching subagent rather than implementing directly:
- UI visual work (icons, color, chrome, theme) -> ui-designer
- UX/interaction/layout/ergonomics -> ux-designer
- audio/Web Audio -> audio-engineer
- [map your other niche agents to their domains]
Use the generalist coder ONLY when no niche agent matches the task, or when a niche agent has attempted it and cannot complete it (then hand off to coder with the specialist's findings).
Always prefer specialist > generalist. State which agent is handling a task before starting.